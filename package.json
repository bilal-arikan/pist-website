{
  "name": "pist-studio-website",
  "version": "1.0.0",
  "description": "Pist Studio — Creative Technology Studio",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve --port=8794",
    "build": "eleventy",
    "spotify": "node scripts/fetch-spotify.mjs",
    "kontrol": "node scripts/kontrol.mjs",
    "yayinla": "npm run build && npm run kontrol && node scripts/yayinla.mjs",
    "spotify-yetki": "node scripts/spotify-yetki.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
