Place your brochure PDF here as `services.pdf`.

Recommended export size: A4 portrait. The viewer fits the page width and allows vertical scroll between pages.

- File path: docs/services.pdf
- Example: docs/services.pdf (not committed by default)
- If the file name or path changes, update `pages/services.html` where `pdfUrl` is set.
