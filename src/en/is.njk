---
pagination:
  data: isler
  size: 1
  alias: is
  # Eleventy varsayılanı yalnızca ilk sayfayı koleksiyona ekliyor;
  # bu olmadan portfolyo sayfalarının 4'ü sitemap'e girmiyordu.
  addAllPagesToCollections: true
permalink: "{{ is.en.url }}"
layout: base.njk
lang: en
extraCss: /assets/css/portfolyo.css
eleventyComputed:
  title: "{{ is.en.baslik }}"
  description: "{{ is.en.aciklama }}"
  alt: "{{ is.tr.url }}"
---
{% include "partials/portfolyo.njk" %}
